class Drops {
    constructor(x,y){
        var options ={
            friction:0.1,
            restitution:0.1
}
 }
    var maxDrops=100;
    for(var i = 0; i<maxDrops;i++){
        drops.push(new createDrop(random(0,400),random(0,400)));
    }
}